# Development — points directly to the live Render backend (CORS is enabled on the server)
# To use a local Flask backend instead, change this to: /api  (requires `npm run dev` + local Flask on :5000)
VITE_BACKEND_URL=http://localhost:5000
