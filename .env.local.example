# Copy this file to .env.local to override the backend URL locally.
# .env.local is git-ignored and takes highest priority.
#
# Example: point to a local backend
# VITE_BACKEND_URL=http://localhost:5000
#
# Example: point directly to a custom Render deploy
# VITE_BACKEND_URL=https://your-custom-backend.onrender.com
