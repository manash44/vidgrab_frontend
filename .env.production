# Production — uses /api as a same-origin prefix, proxied by Netlify to the Render backend.
# This is REQUIRED for mobile browsers (iOS Safari blocks cross-origin <a download> links).
VITE_BACKEND_URL=/api
